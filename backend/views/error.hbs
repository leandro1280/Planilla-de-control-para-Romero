<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8 text-center">
            <div class="card border-0 shadow-sm">
                <div class="card-body p-5">
                    <i class="bi bi-exclamation-triangle text-danger display-1 mb-4"></i>
                    <h1 class="mb-3">Algo salió mal</h1>
                    <p class="lead text-muted mb-4">{{message}}</p>

                    {{#if error.stack}}
                    <div class="text-start bg-light p-3 rounded mb-4 overflow-auto" style="max-height: 300px;">
                        <pre class="small mb-0"><code>{{error.stack}}</code></pre>
                    </div>
                    {{/if}}

                    <a href="/dashboard" class="btn btn-primary btn-lg">
                        <i class="bi bi-house-door me-2"></i>Volver al Inicio
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>