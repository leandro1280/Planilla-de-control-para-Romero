<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}} | Romero Panificados</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link
    href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&family=Poppins:wght@400;500;600;700&display=swap"
    rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="stylesheet" href="/css/mobile-responsive.css">
  <link rel="icon" href="/img/favicon.png" type="image/png">
</head>

<body>
  <div style="position: relative; z-index: 1030; overflow: visible;">
    {{> navbar}}
  </div>

  <div class="container-fluid py-4 px-3 px-md-5" style="position: relative;">
    {{#if messages.success}}
    <div class="alert alert-success alert-dismissible fade show" role="alert">
      {{messages.success}}
      <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
    {{/if}}

    {{#if messages.error}}
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
      {{messages.error}}
      <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
    {{/if}}

    {{{body}}}
  </div>

  {{> footer}}

  <!-- Modal Escáner QR Universal (Fullscreen en móvil) -->
  <div class="modal fade" id="universalQrModal" tabindex="-1" aria-hidden="true" data-bs-backdrop="static" data-bs-keyboard="false">
    <div class="modal-dialog modal-fullscreen-md-down modal-dialog-centered">
      <div class="modal-content qr-scanner-modal">
        <div class="modal-header border-0 bg-dark text-white position-absolute top-0 start-0 end-0 z-3">
          <h5 class="modal-title fw-bold d-flex align-items-center">
            <i class="bi bi-qr-code-scan me-2"></i>
            <span class="d-none d-sm-inline">Escanear Código QR</span>
            <span class="d-inline d-sm-none">Escáner QR</span>
          </h5>
          <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Cerrar"></button>
        </div>
        <div class="modal-body p-0 position-relative" style="min-height: 70vh;">
          <!-- Instrucciones (solo en desktop) -->
          <div class="alert alert-info d-none d-md-flex align-items-center m-3 position-absolute top-0 start-0 end-0 z-2" style="margin-top: 60px !important;" role="alert">
            <i class="bi bi-info-circle-fill me-2 fs-5"></i>
            <div class="small">
              <strong>Escáner Universal</strong><br>
              Escanea códigos QR de fabricantes o tus propios productos. El sistema intentará encontrar el producto automáticamente.
            </div>
          </div>
          
          <!-- Resultado del escaneo -->
          <div id="universal-qr-result" class="position-absolute top-0 start-0 end-0 z-3 m-3 d-none" style="margin-top: 60px !important;"></div>
          
          <!-- Contenedor del escáner -->
          <div id="universal-qr-scanner" class="w-100 h-100 d-flex align-items-center justify-content-center position-relative" style="min-height: 70vh;">
            <!-- Indicador de carga -->
            <div class="text-center text-white position-absolute z-2" id="qr-loading">
              <div class="spinner-border mb-3" role="status">
                <span class="visually-hidden">Cargando cámara...</span>
              </div>
              <p class="small">Iniciando cámara...</p>
            </div>
            <!-- El escáner se inicializará aquí -->
          </div>
          
          <!-- Guía visual para móvil -->
          <div class="qr-guide d-none d-md-block position-absolute top-50 start-50 translate-middle z-2">
            <div class="qr-guide-box"></div>
          </div>
        </div>
        <div class="modal-footer border-0 bg-dark text-white position-absolute bottom-0 start-0 end-0 z-3">
          <button type="button" class="btn btn-light btn-sm" data-bs-dismiss="modal">
            <i class="bi bi-x-circle me-1"></i>Cerrar
          </button>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
    crossorigin="anonymous"></script>
  {{#if usuario}}
  {{#if (eq usuario.rol 'administrador')}}
  <script src="/js/notificaciones.js"></script>
  {{/if}}
  <script src="https://cdn.jsdelivr.net/npm/html5-qrcode@2.3.8/html5-qrcode.min.js"></script>
  <script>
    // Verificar que Html5Qrcode esté disponible
    if (typeof Html5Qrcode === 'undefined') {
      console.error('❌ Html5Qrcode no está cargado. Verifica la conexión a internet o el CDN.');
    } else {
      console.log('✅ Html5Qrcode cargado correctamente');
    }
  </script>
  <script src="/js/qr-universal.js"></script>
  {{/if}}
  <script src="/js/app.js"></script>
</body>

</html>